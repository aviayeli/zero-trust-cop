# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Build artifacts
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/

# Environment / secrets
.env

# Google OAuth artifacts — bearer credentials, never committed
token.json
credentials.json
config/*/token.json
config/*/credentials.json
credentials.json
client_secret_*.json
*:Zone.Identifier

# Ed25519 private key material
*.pem
config/*/signing_key*
!config/*/signing_key.pem.example

# Claude Code local config and throwaway Builder worktrees
.claude/
