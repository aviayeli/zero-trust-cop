# Expected format: an Ed25519 private key in PKCS8 PEM form.
# The file is delimited by BEGIN PRIVATE KEY and END PRIVATE KEY marker lines.
# The real file is named signing_key.pem, is git-ignored, and must never be committed.
